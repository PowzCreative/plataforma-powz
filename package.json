{
  "name": "powz-lead-radar-v7-final",
  "version": "7.0.0",
  "private": true,
  "description": "PowZ Lead Radar v7 final - Jobs + Clients backend",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check api/jobs.js && node --check api/clients.js && node --check api/lib/client-scoring.js"
  },
  "engines": {
    "node": ">=18"
  }
}
